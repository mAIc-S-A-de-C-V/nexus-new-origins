fastapi>=0.115.0
uvicorn[standard]==0.30.3
pydantic==2.8.2
httpx==0.27.0
# Scrapling — adaptive HTML scraping with auto-restoring selectors,
# async fetcher, and built-in stealth (Camoufox is opt-in to keep the
# image lean). See https://github.com/D4Vinci/Scrapling
scrapling>=0.2,<1.0
# Required by Scrapling's AsyncFetcher when stealthy_headers=True — provides
# browser-grade TLS fingerprinting via libcurl-impersonate.
curl_cffi>=0.7,<1.0
# Scrapling imports the playwright Python package at module load time even
# when only AsyncFetcher (the non-browser path) is used. The Python package
# alone is ~50MB; we deliberately do NOT run `playwright install` so no
# browser binaries get downloaded — that's only needed for StealthyFetcher.
playwright>=1.45,<2.0
lxml>=5.2
beautifulsoup4>=4.12
