fastapi>=0.115.0
uvicorn[standard]==0.30.3
pydantic==2.8.2
httpx==0.27.0
# Scrapling — adaptive HTML scraping with auto-restoring selectors,
# async fetcher, and built-in stealth (Camoufox is opt-in to keep the
# image lean). See https://github.com/D4Vinci/Scrapling
# Use the [fetchers] extra — Scrapling 0.4+ moved curl_cffi, playwright,
# browserforge, msgspec, etc. behind this opt-in extra. Installing bare
# scrapling lets imports fail at runtime as soon as you touch a fetcher.
# We deliberately do NOT run `playwright install` afterwards, so no browser
# binaries get downloaded (~250MB saved); only StealthyFetcher needs them
# and we don't use that path in production.
scrapling[fetchers]>=0.4,<1.0
lxml>=5.2
beautifulsoup4>=4.12
