fastapi==0.115.0
starlette>=0.40.0
uvicorn[standard]==0.30.6
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
httpx==0.27.2
pydantic==2.9.2
apscheduler==3.10.4
python-jose[cryptography]>=3.3.0
cryptography>=42.0.0
psutil==6.0.0

# Stats and ML core
numpy==1.26.4
scipy==1.13.1
pandas==2.2.2
statsmodels==0.14.2
scikit-learn==1.5.1
lightgbm==4.5.0
shap==0.46.0
umap-learn==0.5.6
hdbscan==0.8.38
lifelines==0.29.0
mlxtend==0.23.1
prefixspan==0.5.2
ruptures==1.1.9

# Record linkage / fuzzy matching
recordlinkage==0.16
jellyfish==1.1.0

# Causal inference
dowhy==0.11.1

# Phase 15: LLM titles + recommendations (gated by llm_titles_enabled in cfg)
anthropic==0.34.2

# Optional, heavy — loaded only if embeddings_enabled is true at runtime.
# Kept commented to keep the base image lean; uncomment in Phase 10 if you
# want it baked in.
# sentence-transformers==3.0.1
